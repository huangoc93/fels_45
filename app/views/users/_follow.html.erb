<%= form_tag({controller: :relationships, action: :create},
      method: :post, remote: true) do %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= submit_tag "Follow", class: "btn btn-md btn-primary" %>
<% end %>
