<tr>
  <td><%= word.id %></td>
  <td><%= word.name %></td>
  <td><%= "Meaning Here" %></td>
  <td><%= word.category.name %></td>
  <td><%= link_to "edit", edit_admin_word_path(word) %>
      <%= link_to "delete", admin_word_path(word), method: :delete,
                                  data: {confirm: "Are you sure?"} %></td>
</tr>