<tr>
  <td><%= vocabulary.id %></td>
  <td><%= vocabulary.spell %></td>
  <td><%= "Meaning Here" %></td>
  <td><%= vocabulary.category.name %></td>
  <td><%= link_to "edit", edit_admin_vocabulary_path(vocabulary) %>
      <%= link_to "delete", admin_vocabulary_path(vocabulary), method: :delete,
                                  data: {confirm: "Are you sure?"} %></td>
</tr>